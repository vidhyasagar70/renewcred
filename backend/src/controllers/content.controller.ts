import { Request, Response } from 'express';

/**
 * Placeholder content controller.
 * Implement CRUD business logic in follow-up iterations.
 */

export const getAllContent = async (_req: Request, res: Response): Promise<void> => {
  res.status(501).json({ success: false, message: 'Not implemented yet.' });
};

export const getContentById = async (_req: Request, res: Response): Promise<void> => {
  res.status(501).json({ success: false, message: 'Not implemented yet.' });
};

export const createContent = async (_req: Request, res: Response): Promise<void> => {
  res.status(501).json({ success: false, message: 'Not implemented yet.' });
};

export const updateContent = async (_req: Request, res: Response): Promise<void> => {
  res.status(501).json({ success: false, message: 'Not implemented yet.' });
};

export const deleteContent = async (_req: Request, res: Response): Promise<void> => {
  res.status(501).json({ success: false, message: 'Not implemented yet.' });
};
